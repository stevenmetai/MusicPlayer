<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >
<!--
    <item android:id="@android:id/background" android:drawable="@drawable/bar_process_bg" android:height="4dp">
    </item>
    <item android:id="@android:id/progress" android:height="4dp">
        <scale
            android:drawable="@drawable/bar_process"
            android:scaleWidth="100%" />
    </item>-->

    <!--<item android:id="@android:id/background">-->
        <!--<shape>-->
            <!--<solid android:color="@android:color/background_dark"/>-->
            <!--<corners android:radius="5dp" />-->
        <!--</shape>-->
    <!--</item>-->
    <item android:id="@android:id/background" android:drawable="@drawable/bar_process_bg"/>
    <item android:id="@android:id/progress">
        <scale android:scaleWidth="100%" >
            <shape>
                <solid android:color="@color/seekbar_color"/>
                <corners android:radius="5dp" />
            </shape>
        </scale>
    </item>
</layer-list>